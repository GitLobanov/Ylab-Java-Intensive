version: '3.8'

services:
#  app:
#    image: car_shop_app
#    ports:
#      - "8080:8080"
#    environment:
#      DB_URL: jdbc:postgresql://db:5432/car_shop_db
#      DB_USER: postgres
#      DB_PASSWORD: password
#    depends_on:
#      - db
#    networks:
#      - app-network

  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: car_shop_db
    ports:
      - "5432:5432"
    networks:
      - app-network
    volumes:
      - ./init-db.sh:/docker-entrypoint-initdb.d/init-db.sh
    restart: always


networks:
  app-network:
    driver: bridge

volumes:
  postgres_data:
